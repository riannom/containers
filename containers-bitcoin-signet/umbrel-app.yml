manifestVersion: 1.1
id: containers-bitcoin-signet
storage:
  dataRoot: data
name: Bitcoin signet
tagline: Bitcoin Core node on the signet test network
category: bitcoin
icon: https://raw.githubusercontent.com/riannom/containers/main/containers-bitcoin-signet/icon.svg
version: "1.3.1"
port: 38330
description: >-
  Runs a Bitcoin Core full node on signet, a stable, signed test
  network well suited for application development that needs
  predictable block production without testnet's volatile difficulty.

  Includes the same Home / Insight / Settings dashboard as the official
  Umbrel Bitcoin app, configured for signet. Tor and I2P sidecars are
  not deployed in this build, so those tabs will show as unreachable;
  clearnet sync and RPC work normally on the published RPC port (default
  38332) and P2P port (default 38333). If another installed app already
  uses one of these ports, the app picks the next free one (default +
  100, + 200, ...) when it is installed and keeps it; the dashboard's
  connection details show the ports in use.

  This app stores its data in its own per-app data directory and does
  not affect or share state with the mainnet bitcoind app installed
  from the official Umbrel store.
website: https://github.com/riannom/containers
support: https://github.com/riannom/containers
gallery: []
releaseNotes: >-
  Host ports are now chosen automatically. RPC and P2P use their default
  ports unless another installed app already uses one, in which case the
  app picks the next free port (default + 100, + 200, ...) at install and
  keeps it. This fixes installs failing with "port is already allocated"
  next to apps such as Elements. Existing installs keep their ports.
  After updating, also update Electrs signet.
backupIgnore:
  - data/bitcoin/signet/blocks
  - data/bitcoin/signet/chainstate
  - data/bitcoin/signet/indexes
developer: riannom
repo: https://github.com/riannom/containers
path: ""
defaultUsername: ""
defaultPassword: ""
