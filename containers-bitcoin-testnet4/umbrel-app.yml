manifestVersion: 1.1
id: containers-bitcoin-testnet4
storage:
  dataRoot: data
name: Bitcoin testnet4
tagline: Bitcoin Core node on the testnet4 network (BIP 94)
category: bitcoin
icon: https://raw.githubusercontent.com/riannom/containers/main/containers-bitcoin-testnet4/icon.svg
version: "1.3.1"
port: 58330
description: >-
  Runs a Bitcoin Core full node on testnet4, the modern public Bitcoin
  test network introduced via BIP 94. Recommended over testnet3 for new
  development work targeting current consensus rules.

  Includes the same Home / Insight / Settings dashboard as the official
  Umbrel Bitcoin app, configured for testnet4. Tor and I2P sidecars are
  not deployed in this build, so those tabs will show as unreachable;
  clearnet sync and RPC work normally on the published RPC port (default
  58332) and P2P port (default 58333). These defaults differ from
  testnet4's standard ports (48332/48333) because umbrelOS reserves
  ports 40000-49999. If another installed app already uses one of these
  ports, the app picks the next free one (default + 100, + 200, ...)
  when it is installed and keeps it; the dashboard's connection details
  show the ports in use.

  This app stores its data in its own per-app data directory and does
  not affect or share state with the mainnet bitcoind app installed
  from the official Umbrel store.
website: https://github.com/riannom/containers
support: https://github.com/riannom/containers
gallery: []
releaseNotes: >-
  Host ports are now chosen automatically. RPC and P2P use their default
  ports unless another installed app already uses one, in which case the
  app picks the next free port (default + 100, + 200, ...) at install and
  keeps it. This fixes installs failing with "port is already allocated"
  next to apps such as Elements. Existing installs keep their ports.
  After updating, also update Electrs testnet4.
backupIgnore:
  - data/bitcoin/testnet4/blocks
  - data/bitcoin/testnet4/chainstate
  - data/bitcoin/testnet4/indexes
developer: riannom
repo: https://github.com/riannom/containers
path: ""
defaultUsername: ""
defaultPassword: ""
